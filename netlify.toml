# Netlify Configuration - HTTP API Support
# Netlify allows HTTP fetch from serverless functions!

[build]
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Enable HTTP fetch in functions
[functions]
  node_bundler = "esbuild"
